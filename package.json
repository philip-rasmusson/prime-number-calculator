{
  "homepage": "https://philip-rasmusson.github.io/prime-minister/",
  "name": "prime-minister",
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build",
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "jest"
  },
  "devDependencies": {
    "gh-pages": "^3.1.0"
  }
}
